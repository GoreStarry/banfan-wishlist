import React, {
  useRef,
  useState,
  useCallback,
  useEffect,
  useMemo,
} from "react";
import PropTypes from "prop-types";

import sty from "./Recruiter.module.scss";

const Recruiter = ({ children }) => {
  return <div className={sty.Recruiter}>43123</div>;
};

Recruiter.propTypes = {};

export default Recruiter;
